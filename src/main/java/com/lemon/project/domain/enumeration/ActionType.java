package com.lemon.project.domain.enumeration;

/**
 * The ActionType enumeration.
 */
public enum ActionType {
    URL, INTENT, SEARCH, PENDING_INTENT, START_ACTIVITY, SERVICE, BROADCAST, CONTENT_PROVIDER, OTHER
}
